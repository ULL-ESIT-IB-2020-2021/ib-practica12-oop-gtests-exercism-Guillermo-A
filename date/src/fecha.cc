#include <iostream>
#include <stdlib.h>
#include <fstream>
#include "fecha.h"
#include <vector>

int main(int argc, char*argv[]){
    unsigned _day, _month, _year;
    std::vector<long> my_date();
    FILE *file;
    file = fopen(argv[1], "r");
    std::fstream file;
    take_date(my_date(), file);
    Date My_Date(long date);
    return 0;

}